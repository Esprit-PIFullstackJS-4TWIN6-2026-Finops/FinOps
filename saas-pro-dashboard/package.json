{
  "name": "saas-pro-dashboard",
  "private": true,
  "scripts": {
    "dev": "concurrently -n FRONT,BACK \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\"",
    "migrate": "npm --prefix backend run migrate"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}

