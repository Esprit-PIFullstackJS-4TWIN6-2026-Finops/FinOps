# Frontend (Vite) — copy to `.env` or `.env.local` if needed
# API base URL for the NestJS backend (default: http://192.168.100.189:32343)
VITE_API_URL=http://192.168.100.189:32343

# Optional: Gemini for client-side experiments (see vite.config.ts)
# GEMINI_API_KEY=
